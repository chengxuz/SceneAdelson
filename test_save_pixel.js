var zeros = require("zeros")
var savePixels = require("save-pixels")
var fs = require("fs")

var out = fs.createWriteStream('test_img_ge.png')

//Create an image
//var x = zeros([32, 32])
//x.set(16, 16, 255)

//Save to a file
//savePixels(x, "png").pipe(process.stdout)
//savePixels(x, "png").pipe(out)
console.log(parseFloat('101', 10) + 10)
